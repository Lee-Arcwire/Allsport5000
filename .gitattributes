# Keep the repo LF-normalised (core.autocrlf is on globally on this machine,
# and .vscode/settings.json sets files.eol to LF).
* text=auto eol=lf

# Binary assets
*.png  binary
*.jpg  binary
*.jpeg binary
*.pdf  binary
*.woff2 binary
